const pegaArquivo = require('../index')

test('deve ser uma funcao', () => {
    expect(typeof pegaArquivo).toBe('function')
})

test('deve ser uma funcao', () => {
    expect(typeof pegaArquivo).toBe('function')
})